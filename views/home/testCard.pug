function loadShip() {
let row = document.getElementById('row');
let htmlColor = "#{colorThemeText}";
let edit = "#{edit}";

function loadAdmin() {
  row.insertAdjacentHTML("afterbegin",
  `<div class="col s3" style="min-width: 15em;">
    <div class="card z-depth-2">
      <div class="card-image waves-effect waves-block waves-light">

          <img class="activator responsive-img" id="gambar" src="/" alt="Boat" onerror="this.onerror=null;this.src='/public/img/site/noshippic.png';" style="width:100%;height:14.8em;object-fit: cover;" />

      </div>
      <div class="card-content">
        <span class="card-title activator ${htmlColor}-text text-darken-4" id="nama"><h5 class="truncate"><p class="flow-text">-</p></h5></span>
        <p>
        <a id="shipId" class="btn ${htmlColor}" href="#">Detail</a>
        <a class="btn tooltipped" data-position="bottom" data-tooltip="I am a tooltip">Hover me!</a>
        </p>
      </div>
      <div class="card-reveal"><span class="card-title ${htmlColor}-text text-darken-4" id="model">-<i class="material-icons right">close</i></span>
        <p class="${htmlColor}-text text-darken-4" id="tipe">-</p>
        <p class="${htmlColor}-text text-darken-4" id="harga">-</p>
        <p class="${htmlColor}-text text-darken-4" id="tanggalPembuatan">-</p>
        <p class="${htmlColor}-text text-darken-4" id="panjangKapal">-</p>
      </div>
    </div>
  </div>`);
  
  document.getElementById('shipId').href = "categories/"+shipData[i]._id;
  document.getElementById('gambar').src = shipData[i].picture;
  document.getElementById('nama').firstChild.textContent = shipData[i].name;
  document.getElementById('model').firstChild.textContent = shipData[i].model;
  document.getElementById('tipe').firstChild.textContent = `Tipe Kapal: ${shipData[i].type}`;
  document.getElementById('harga').firstChild.textContent = `Harga Kapal: ${shipData[i].price}`;
  document.getElementById('tanggalPembuatan').firstChild.textContent = `Tanggal Pembuatan: ${shipData[i].details.yearOfManufactured}`;
  document.getElementById('panjangKapal').firstChild.textContent = `Panjang Kapal: ${shipData[i].details.lengthOverall}`;
}

if(userData.isAdmin==true){
  for(i in shipData){

    loadAdmin()
}
}
else{
for(i in shipData){
   
  row.insertAdjacentHTML("afterbegin",
  `<div class="col s3" style="min-width: 15em;">
    <div class="card z-depth-2">
      <div class="card-image waves-effect waves-block waves-light">

          <img class="activator responsive-img" id="gambar" src="/" alt="Boat" onerror="this.onerror=null;this.src='/public/img/site/noshippic.png';" style="width:100%;height:14.8em;object-fit: cover;" />

      </div>
      <div class="card-content">
      <span class="card-title activator ${htmlColor}-text text-darken-4" id="nama"><h5 class="truncate"><p class="flow-text">-</p></h5></span>
        <p><a id="shipId" class="btn ${htmlColor}" href="#">Detail</a></p>
      </div>
      <div class="card-reveal"><span class="card-title ${htmlColor}-text text-darken-4" id="model">-<i class="material-icons right">close</i></span>
        <p class="${htmlColor}-text text-darken-4" id="tipe">-</p>
        <p class="${htmlColor}-text text-darken-4" id="harga">-</p>
        <p class="${htmlColor}-text text-darken-4" id="tanggalPembuatan">-</p>
        <p class="${htmlColor}-text text-darken-4" id="panjangKapal">-</p>
      </div>
    </div>
  </div>`);
  
  document.getElementById('shipId').href = "categories/"+shipData[i]._id;
  document.getElementById('gambar').src = shipData[i].picture;
  document.getElementById('nama').firstChild.textContent = shipData[i].name;
  document.getElementById('model').firstChild.textContent = shipData[i].model;
  document.getElementById('tipe').firstChild.textContent = `Tipe Kapal: ${shipData[i].type}`;
  document.getElementById('harga').firstChild.textContent = `Harga Kapal: ${shipData[i].price}`;
  document.getElementById('tanggalPembuatan').firstChild.textContent = `Tanggal Pembuatan: ${shipData[i].details.yearOfManufactured}`;
  document.getElementById('panjangKapal').firstChild.textContent = `Panjang Kapal: ${shipData[i].details.lengthOverall}`;
}
}
};
addLoadEvent(loadShip)
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  doctype html
  html(lang="en")
    include ../includes/head.pug
    title Sewa Kapal
    body
      include ../includes/header.pug  
      .container
        .section
          .row#row
            each i in `${shipData}`
              .col.s3(style='min-width: 15em;')
                .card.z-depth-2
                  .card-image.waves-effect.waves-block.waves-light
                    img#gambar.activator.responsive-img(src='/', alt='Boat', onerror="this.onerror=null;this.src='/public/img/site/noshippic.png';", style='width:100%;height:14.8em;object-fit: cover;')
                  .card-content
                    span#nama.card-title.activator.text-darken-4(class='${htmlColor}-text')
                      h5.truncate
                        - var te = "test" // TODO: how to use variable from script tag, atm use ajax on this file and try to use it in pug line 
                        p.flow-text #{te}
                    p
                      a#shipId.btn(class='${htmlColor}', href='#') Detail
                    .a.btn.tooltipped(data-position='bottom', data-tooltip='I am a tooltip') Hover me!
                    p
                  .card-reveal
                    span#model.card-title.text-darken-4(class='${htmlColor}-text')
                      | -
                      i.material-icons.right close
                    p#tipe.text-darken-4(class='${htmlColor}-text') -
                    p#harga.text-darken-4(class='${htmlColor}-text') -
                    p#tanggalPembuatan.text-darken-4(class='${htmlColor}-text') -
                    p#panjangKapal.text-darken-4(class='${htmlColor}-text') -


      include ../includes/footer.pug
      include ../includes/loginmodal.pug
      include ../includes/script.pug  

      script(src='/public/js/shipInit.js')
      script.
            function loadShip() {
            let row = document.getElementById('row');
            let htmlColor = "#{colorThemeText}";
            let edit = "#{edit}";
            var test = "test";
            
                document.getElementById('shipId').href = "categories/"+shipData[0]._id;
                document.getElementById('gambar').src = shipData[0].picture;
                document.getElementById('nama').firstChild.textContent = shipData[0].name;
                document.getElementById('model').firstChild.textContent = shipData[0].model;
                document.getElementById('tipe').firstChild.textContent = `Tipe Kapal: ${shipData[0].type}`;
                document.getElementById('harga').firstChild.textContent = `Harga Kapal: ${shipData[0].price}`;
                document.getElementById('tanggalPembuatan').firstChild.textContent = `Tanggal Pembuatan: ${shipData[0].details.yearOfManufactured}`;
                document.getElementById('panjangKapal').firstChild.textContent = `Panjang Kapal: ${shipData[0].details.lengthOverall}`;
            };
          addLoadEvent(loadShip)

      
